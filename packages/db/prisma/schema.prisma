generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Placeholder model — full schema comes in Task 02
model Placeholder {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
}
